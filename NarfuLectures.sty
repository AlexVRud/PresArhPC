\NeedsTeXFormat{LaTeX2e}[1994/06/01]
\ProvidesPackage{NarfuLectures}[2017/02/25 NARFU Lectures]

\RequirePackage{GoogleColorPalette}
\RequirePackage{tikz}

\ProcessOptions\relax

%
% TikZ
%

\usetikzlibrary{arrows}
\usetikzlibrary{arrows.meta}
\usetikzlibrary{babel}
\usetikzlibrary{decorations.pathreplacing}
\usetikzlibrary{fit}
\usetikzlibrary{shapes}
\usetikzlibrary{shapes.geometric}
\usetikzlibrary{positioning}
\usetikzlibrary{patterns}
\usetikzlibrary{shadows.blur}

\newcommand*{\ByteWidth}{0.9cm}
\newcommand*{\ByteHeight}{0.6cm}
\newcommand*{\DeltaWidth}{0.05cm}

\tikzset{
%
% Colors (fill)
%
  greencolor/.style={
    top color=Green50,
    bottom color=Green200,
  },
  redcolor/.style={
    top color=Red50,
    bottom color=Red200,
  },
  bluecolor/.style={
    top color=Blue50,
    bottom color=Blue200,
  },
  yellowcolor/.style={
    top color=Yellow50,
    bottom color=Yellow200,
  },
  orangecolor/.style={
    top color=Orange50,
    bottom color=Orange200,
  },
  BitColorBase/.style={
    top color=Yellow400,
    bottom color=Yellow100,
  },
  BitColorError/.style={
    top color=Red400,
    bottom color=Red100,
  },
  BitColorChange/.style={
    top color=Green400,
    bottom color=Green100,
  },
  BitColorHide/.style={
    top color=Gray300,
    bottom color=Gray100,
  },
  BitSelected/.style={
    draw=Red800,
    very thick,
  },
%
% Style for char
%
  MyChar/.style={
    rectangle,
    rounded corners=1mm,
    minimum height=6mm,
    minimum width=4.8mm,
    very thick,
    draw=Gray600,
    top color=white,
    bottom color=Gray200,
  },
%
% Style for bit
%
  bit/.style={
    draw=Gray700,
    BitColorBase,
    minimum height=0.6cm,
    minimum width=0.6cm,
    rounded corners=0.05cm,
  },
%
% Style for butes
%
  databox/.style={draw,fill=white,minimum width=#1*\ByteWidth, minimum height=\ByteHeight, path picture={%
    \ifnum#1>1
      \foreach \x in {1,...,#1}%
        \draw (path picture bounding box.south west) ++(\x*\ByteWidth,0) -- ++(0,0.15*\ByteHeight);%
    \fi
    }
  },
  databox/.default=4,
%
% Flowchart
%
  startstop/.style={
    rectangle,
    rounded corners=0.4cm,
    minimum width=3cm,
    minimum height=1cm,
    text centered,
    draw=black,
    redcolor,
  },
  io/.style={
    trapezium,
    trapezium left angle=70,
    trapezium right angle=110,
    minimum width=3cm,
    minimum height=1cm,
    text centered,
    draw=black,
    bluecolor,
  },
  process/.style={
    rectangle,
    minimum width=3cm,
    minimum height=1cm,
    text centered,
    draw=black,
    orangecolor,
  },
  decision/.style={
    diamond,
    minimum width=3cm,
    minimum height=1cm,
    text centered,
    draw=black,
    greencolor,
  },
  arrow/.style={
    thick,
    >=stealth,
  },
%
% Process
%
  file/.style={
    rectangle,
    draw=black,
    fill=white,
    minimum width=2cm,
    minimum height=1cm,
  },
  program/.style={
    ellipse,
    draw=black,
    bluecolor,
  },
  state/.style={
    rectangle,
    draw=gray,
    very thick,
    rounded corners,
  },
  command/.style={
    ellipse,
    draw=gray,
    very thick,
    fill=blue!20!white
  },
  myArrow/.style={
    draw=gray,
    very thick,
    >=angle 90,
  },
}

%
% Beamer
%

\AtBeginSection[]{
  \begin{frame}
  \vfill
  \centering
  \begin{beamercolorbox}[sep=8pt,center,shadow=true,rounded=true]{title}
    \usebeamerfont{title}\secname\par%
  \end{beamercolorbox}
  \vfill
  \end{frame}
}

\endinput

